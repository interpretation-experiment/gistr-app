<div class="form-group {{if error 'has-error'}}">
  <label for="{{inputId}}" class="control-label">{{yield}}</label>
  {{focused-textarea
      id=inputId
      class="form-control"
      value=value
      rows="1" cols="50"
      disabled=disabled
      autoresize=true
      }}
  <span class="help-block">{{error}}</span>
</div>
